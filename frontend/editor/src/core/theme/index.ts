/**
 * xiaoyao-pdf theme barrel exports
 * Consumers: `import { tokens, MantineProvider } from '@app/theme'`
 */

export * from './tokens';
export { MantineProvider, mantineTheme } from './MantineProvider';